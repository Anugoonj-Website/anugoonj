import PreviousMap from "postcss/lib/previous-map";
import React from "react";
import "../styles/milestones.css";

const Cardcarousel = (props) => {
  return <div className="cards">img {props.cardno}</div>;
};

export default Cardcarousel;
